import React from "react";

/**
 * Deprecated TimeEntry form retained only for import compatibility.
 *
 * FormEngine is the current TimeEntry UI. This legacy component previously
 * contained direct browser writes to TimeEntry, ReportFieldAnswer, and Meeting.
 * It must remain inert until a server-authorized replacement is explicitly
 * designed and migrated.
 */
export default function TimeEntryForm() {
  return null;
}
