/**
 * AccessRequestsPanel — DISABLED
 *
 * The app uses invite-only access. Self-service access requests are not allowed.
 * PendingRoleAssignment (created by admin invite) is the sole approval mechanism.
 * This panel is intentionally empty — no AccessRequest records will be created.
 */
export default function AccessRequestsPanel() {
  return null;
}