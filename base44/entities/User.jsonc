{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "management",
        "employee",
        "client",
        "pre_ets",
        "dspd",
        "pre_ets_employer",
        "ce_instructor",
        "ce_student"
      ],
      "default": "employee",
      "description": "User's role. 'user' is the platform default before onUserRegistered upgrades it. ce_instructor and ce_student are dedicated to CE Training Portal only."
    },
    "access_level": {
      "type": "string",
      "enum": [
        "client_portal",
        "staff",
        "admin",
        "pre_ets_employer_portal",
        "ce_training_portal"
      ],
      "description": "Access level granted via invitation. Blank = denied everywhere. ce_training_portal = CE Training Portal only."
    },
    "is_active": {
      "type": "boolean",
      "default": true,
      "description": "False when the user has been deactivated/offboarded. Blocks app access even if role/access_level are set."
    },
    "org_id": {
      "type": "string",
      "description": "Tenant organization ID"
    },
    "manager_id": {
      "type": "string",
      "description": "ID of the manager/admin who invited this user"
    },
    "linked_client_id": {
      "type": "string",
      "description": "Client record ID linked to this user (for client portal users)"
    },
    "phone": {
      "type": "string"
    },
    "title": {
      "type": "string"
    },
    "avatar_url": {
      "type": "string"
    },
    "timezone": {
      "type": "string"
    },
    "cohort_id": {
      "type": "string",
      "description": "CE Training cohort ID (for ce_instructor and ce_student roles)"
    },
    "cohort_role": {
      "type": "string",
      "enum": [
        "instructor",
        "student"
      ],
      "description": "Role within the CE Training cohort (instructor manages, student participates)"
    }
  },
  "required": []
}